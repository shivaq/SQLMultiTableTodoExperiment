import java.util.Calendar;

CREATE TABLE todo(
  _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  todo TEXT NOT NULL,
  registerd_date INTEGER NOT NULL,
  updated_date INTEGER NOT NULL

);

insert_todo:
INSERT INTO todo(todo, registerd_date, updated_date)
VALUES(?,?,?);

select_all:
SELECT *
FROM todo;